# Resend API key — get it at resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# Where quote emails get delivered
TO_EMAIL=contact@50statemovers.com

# Port — Render sets this automatically; only needed for local dev
PORT=3000
